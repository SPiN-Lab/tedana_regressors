codecov:
  notify:
    require_ci_to_pass: yes

coverage:
  status:
    project:
      default:
        # basic
        target: 0
        threshold: 0
    patch:
      default:
        target: 0
        threshold: 0

ignore:
  - "tedana_regressors/tests/"
  - "tedana_regressors/_version.py"
  - "tedana_regressors/info.py"
